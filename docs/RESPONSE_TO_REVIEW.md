# 对 REVIEW_REPORT.md 的逐条回应与处置

日期: 2026-08-18。所有修复已同步到 PAPER_DRAFT.md、paper/main.tex（重新编译）
与公开仓库 github.com/lshhhhhhh/pseudoforest-counterexample。

## 必须修改（全部接受，全部完成）

**M1 嵌套树定义不一致 — 接受，已修。**
审查完全正确：Lemma 2.4 字面上把嵌套树定义为"区域×曲线二分关联图"，与
Lemma 2.5 的 I（区域为点、曲线为边）及 Proposition 6.2 的二染色论证不一致；
按二分图读法 6.2 的染色论证字面无效。已统一定义为区域邻接树（顶点=区域，
边=曲线），Lemma 2.4 陈述、证明措辞、"nesting tree"定义句、Lemma 2.5 结尾
同步改写。b_R、Cor 2.7、Theorem A 记账不受影响（归纳证明原本就是按区域树
写的，错在陈述句）。TeX+MD 均已修，PDF 重编译。

**M2 定理 B 审计包不自包含 — 接受，已修（六个子项逐一处置）。**
1. 解析静默跳行 → 新脚本 `scripts/theoremB_audit.py` 严格解析，畸形行抛异常；
2. plantri 退出码/stderr → 新脚本检查 returncode 并捕获 stderr；
3. 只信 solve() 布尔值 → 新脚本对每个 SAT 实例取 get_model()，解码定向后
   **直接数每点出入度**验证 min(d+,d-)<=1（独立于编码正确性）；standalone
   verifier 的阳性对照同样升级为模型度数直查；
4. n=13..17 日志未归档 → 全量 n=4..17 用审计脚本**重新跑一遍**（n>=14 分
   14 片），逐阶/逐片日志写入仓库 `verification/theoremB/`，计数对
   A000109 断言；
5. "re-derives everything" 过强 → 论文改为精确列举 verifier 的五项检查，
   并明示其不覆盖定理 B（定理 B 的审计包单独归档）；
6. 无仓库 URL/哈希/重跑说明 → `verification/ENVIRONMENT.txt` 记录 plantri
   SHA256、软件版本、精确命令与驱动脚本；仓库即公开 URL，提交哈希由 git
   自证。

**I1 "本方案内 58 最优"过强 — 接受，已按建议弱化。**
现表述为：在"每片段取 Gamma'−v、Gamma' 为非哈密顿三连通三次平面图"的
子类内 58 最优。（注：一般方案的最优性可望经由"三次图 3-边割侧收缩保持
三连通"的标准事实恢复，但这需要额外引理，本稿不作此宣称。）

**I2 "38 点恰有六个"归属 — 接受，已修。**
增引 Aldred–Bau–Holton–McKay, SIAM J. Discrete Math. 13 (2000) 25–32
（六个极值图的完整清单）；[HM] 仅用于最小阶 38。正文相应改写。

## 重要问题

**I3 "独立复核"措辞 — 接受，已修。**
第 5 节开头改为三层证据分类（正文证明 / 仓库可重跑检查 / 未归档的内部
复核仅作过程说明）；AI 披露中"without trusting any component of our
pipeline"改为"不信任我们自己的管线（标准库与 SAT 求解器当然仍需信任）"。

## 次要修订（全部落实）

1. "3-connected by Whitney" → "简单极大平面图（>3 点）自动三连通"；
2. 脚本 27 文档串更新（强性质为假，454/1893，主证明不依赖它）；
3. 图 1 的 B_i → F_i（论文图本已是 F_i；研究目录旧图已重渲染统一）；
4. 第 6 节数据点段：正式定义 flip（与 F/M 交错圈的对称差），标注数据日志
   位置（verification/flip_landscape/），措辞降为 computational observation；
5. [RW] 已在正文背景处引用；
6. TODO 清单只存在于工作版 MD，TeX 投稿版本无；
7. [SAT] 参考文献拆分为 PySAT / CaDiCaL / Glucose 三条完整条目。

## 关于第五节的"容量论证"压缩建议

审查给出的 z+w<=1 容量重述与现 Steps 4–6 等价且更紧凑，作为可选重构记录
在案；本轮保守起见保留已通过两轮独立审查的分步版本，投稿修订轮若篇幅
需要再采用。

## 与第一轮（敌意子代理）审查的关系

两轮审查结论一致：定理 A 的数学无致命漏洞。第一轮抓住了计数错误
（149,960,273）、六图/引用互换等；本轮抓住了嵌套树定义打架（第一轮按
"意图读法"放行，本轮按字面读法拒绝——后者是对的标准）与审计包缺口。
两轮互补，均已全部落实。
